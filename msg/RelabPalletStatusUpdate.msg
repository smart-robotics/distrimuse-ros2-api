# Update about the status of a pallet after operator action

string name  # name of pallet (either "left" or "right")
bool pallet_restored  # True if pallet is restored (either error recovered or replaced with empty pallet)
